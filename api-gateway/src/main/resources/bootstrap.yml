eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8086

spring:
  application:
    name: "API-GATEWAY"
  config:
      import:
        optional:
          configserver:http://localhost:8888

